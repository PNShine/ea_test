/**
 * Utility function that checks if object is null or undef
 * @param {*} object the victim object
 */
export const isNullOrUndefined = (object) =>{
  return (object === null || object === undefined);
}